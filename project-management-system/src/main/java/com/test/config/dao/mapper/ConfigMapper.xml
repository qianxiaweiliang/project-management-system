<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.config.dao.ConfigDao">

  <insert id="addTestUser">
    INSERT INTO config (kind,status) VALUES(#{kind},#{status})
  </insert>
  
  <insert id="addTestUser1" parameterType="com.test.config.entity.ConfigEntity">
    INSERT INTO config (kind,description) VALUES(#{kind},#{description})
  </insert>
</mapper>